<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" 
    href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" 
    integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" 
    crossorigin="anonymous">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>FoodGo</title>
</head>
<body>
    <div class="container">
        <h1 class="mb-4">Food Selection</h1>
        <a href="/foods/new" class="btn btn-success">New Food Item</a>

        <% foods.forEach(food => { %>
            <div class="card mt-4">
                <div class="card-body">
                    <h4 class="card-title"><%= food.title %></h4>
                    <div class="card-subtitle text-muted mb-2">
                        <%= food.createdAt.toLocaleDateString() %>
                    </div>
                    <div class="card-text mb-2"><%= food.description %></div>
                    <a href="foods/<%= food.slug %>" 
                    class="btn btn-primary">Read More</a>
                </div>
            </div>
        <% }) %>
    </div>
</body>
</html>